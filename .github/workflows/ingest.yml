name: Ingest catalogue

# Run by hand from the Actions tab. Ingestion reaches out to a third-party
# site, so it is never automatic — someone decides each time.
on:
  workflow_dispatch:
    inputs:
      source:
        description: "Where to ingest from"
        type: choice
        options: ["librivox", "html"]
        default: "librivox"
      limit:
        description: "How many books at most (large numbers make a large JSON file)"
        default: "200"
      language:
        description: "librivox only — language filter (e.g. English, Turkish). Blank = all"
        default: "Turkish"
      base_url:
        description: "html only — catalogue site"
        default: "https://seslikitab.org"
      catalog_path:
        description: "html only — page the crawl starts from"
        default: "/"
      book_url_pattern:
        description: "html only — which paths are book pages (regex)"
        default: "/(kitap|book|audiobook|sesli-kitap)/[^/]+/?$"
      pages:
        description: "html only — how many catalogue pages to walk"
        default: "5"

jobs:
  ingest:
    runs-on: ubuntu-latest

    permissions:
      # The exported catalogue is committed back to the branch.
      contents: write

    env:
      # A throwaway database: the crawl fills it, the export reads it, and the
      # runner is discarded. Nothing here touches the deployed database.
      DATABASE_URL: "postgresql://monk:monk@localhost:5432/monk"
      INGEST_DELAY_MS: "1000"
      INGEST_USER_AGENT: "MONK/0.1 (+https://github.com/${{ github.repository }})"
      # Only read by the html source; librivox ignores them.
      INGEST_BASE_URL: ${{ inputs.base_url }}
      INGEST_CATALOG_PATH: ${{ inputs.catalog_path }}
      INGEST_BOOK_URL_PATTERN: ${{ inputs.book_url_pattern }}

    services:
      postgres:
        image: postgres:16
        env:
          POSTGRES_USER: monk
          POSTGRES_PASSWORD: monk
          POSTGRES_DB: monk
        ports: ["5432:5432"]
        # Steps must not start before the server accepts connections.
        options: >-
          --health-cmd pg_isready
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5

    steps:
      - uses: actions/checkout@v4
        with:
          ref: ${{ github.ref_name }}

      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm

      - run: npm ci

      - name: Create the database
        run: npm run db:migrate

      - name: Ingest
        run: |
          npm run ingest -- \
            --source "${{ inputs.source }}" \
            --limit "${{ inputs.limit }}" \
            --pages "${{ inputs.pages }}" \
            ${{ inputs.language && format('--language "{0}"', inputs.language) || '' }} \
            --write

      - name: Export the catalogue
        run: npm run catalog:export

      # Always: a failed run is exactly when the report is worth reading. When
      # the export step broke, this was skipped and the run looked silent.
      - name: Report
        if: always()
        run: |
          {
            echo "### Ingestion result"
            echo ""
            echo '```'
            tail -n 40 logs/ingest.jsonl 2>/dev/null || echo "(no report file)"
            echo '```'
          } >> "$GITHUB_STEP_SUMMARY"

      - name: Upload the run report
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: ingest-report
          path: logs/ingest.jsonl
          if-no-files-found: ignore

      - name: Commit the catalogue
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add prisma/seed-data/catalog.json

          if git diff --cached --quiet; then
            echo "Catalogue unchanged; nothing to commit."
            exit 0
          fi

          COUNT=$(node -e "console.log(require('./prisma/seed-data/catalog.json').books.length)")
          git commit -m "Ingest ${COUNT} books from ${{ inputs.source }}"
          git push
